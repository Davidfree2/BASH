Gitcut

a simple cli command that automates github add all, commit, and commit message in one.
just move this file to ./local/bin
then run chmod +x on the file 
after that you should be able to run gitcut from anywhere
